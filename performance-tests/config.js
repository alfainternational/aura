// تكوين اختبارات الأداء
export const config = {
  // عنوان API الخاص بتطبيق أورا
  baseUrl: 'http://localhost:8000/api',
  
  // إعدادات المستخدمين لاختبارات التحميل
  users: {
    // عدد المستخدمين المتزامنين
    concurrentUsers: {
      low: 10,      // منخفض
      medium: 50,   // متوسط
      high: 100,    // مرتفع
      extreme: 200  // مرتفع جدًا
    },
    
    // معدل وصول المستخدمين الجدد (مستخدم/ثانية)
    arrivalRate: {
      low: 1,       // منخفض
      medium: 5,    // متوسط
      high: 10,     // مرتفع
      extreme: 20   // مرتفع جدًا
    }
  },
  
  // مدة الاختبار (بالثواني)
  duration: {
    short: 60,      // قصير (دقيقة واحدة)
    medium: 300,    // متوسط (5 دقائق)
    long: 900       // طويل (15 دقيقة)
  },
  
  // أهداف الأداء
  thresholds: {
    // الحد الأقصى لزمن الاستجابة (بالمللي ثانية)
    responseTime: {
      p95: 500,     // 95% من الطلبات يجب أن تكون أقل من 500 مللي ثانية
      p99: 1000     // 99% من الطلبات يجب أن تكون أقل من 1000 مللي ثانية
    },
    
    // الحد الأدنى لمعدل الطلبات الناجحة
    successRate: 0.95  // 95% من الطلبات يجب أن تنجح
  }
};
